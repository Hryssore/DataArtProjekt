# Frontend

React chat client with routing, auth screens, room and dialog views, session management, unread badges, moderation modals, and Socket.IO integration.

## Startup

```bash
npm install
npm run dev
```

Environment values are documented in [frontend/.env.example](./.env.example).
