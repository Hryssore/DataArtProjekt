# Backend

Express, PostgreSQL, and Socket.IO backend for the classic web chat project.

## Startup

```bash
npm install
npm run migrate
npm run dev
```

The service expects PostgreSQL credentials through environment variables. See [backend/.env.example](./.env.example).
